<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Gerekli İzinler -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <application
        android:name="${applicationName}"
        android:label="yusuf_yemek"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name="com.example.yusuf_yemek.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">

            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"
                />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- DÜZELTME: Silinen API Anahtarı geri eklendi. -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBqjtFWFy9hmFoV8rscyB-VRFv4qS2vVHI"/>

        <!-- Flutter'ın eklentileri düzgün çalıştırması için gerekli meta-veri -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

    </application>

    <!-- Metin işleme gibi bazı native özellikleri sorgulamak için gerekli -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>

</manifest>
